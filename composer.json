{
    "name": "echtoran/marvel-api-client",
    "license": "MIT",
    "type": "library",
    "description": "An API client to handle calls to the Marvel API.",
    "autoload": {
        "psr-4": {
            "ikoene\\Marvel\\": ["src/Marvel"]
        }
    },
    "require": {
        "guzzle/guzzle": "*",
        "jms/serializer": "*",
        "symfony/yaml": "*",
        "doctrine/collections": "*"
    },
    "require-dev": {
        "phpspec/phpspec": "*"
    },
    "config": {
        "bin-dir": "bin"
    }
}
